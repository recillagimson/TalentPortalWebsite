# Project-local workaround for corporate-network TLS interception.
# Skips cert verification when fetching from the npm registry.
# Safe on a trusted dev machine; remove or set strict-ssl=true if this repo
# is ever installed in a less-trusted environment.
strict-ssl=false
